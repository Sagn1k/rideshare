package tests

import (
	// "net/http"
	// "net/http/httptest"
	// "rideshare/handlers"
	"testing"
)

func TestIntegration(t *testing.T) {
    // app := fiber.New()
    // // Initialize routes
    // handlers.InitializeRoutes(app)

    // // Test adding user
    // reqUser := httptest.NewRequest("POST", "/user", nil)
    // reqUser.Header.Set("Content-Type", "application/json")
    // respUser := httptest.NewRecorder()
    // app.ServeHTTP(respUser, reqUser)
    // if respUser.Code != http.StatusCreated {
    //     t.Errorf("Expected status code %d but got %d", http.StatusCreated, respUser.Code)
    // }

    // // Similar tests for other endpoints
}
